import React, { useState } from "react";

interface Iprops {
    current:number;
    length: number
}

const PCList = (props: Iprops) => {
    const {current, length} = props;


    return <div>
        todo: PCList
    </div>

}

export default PCList