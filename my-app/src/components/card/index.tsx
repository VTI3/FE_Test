import React, { useState } from "react";

interface Iprops {
    current:number;
    length: number
}

const Card = (props: Iprops) => {
    const {current, length} = props;


    return <div>
        todo: Card
    </div>

}

export default Card